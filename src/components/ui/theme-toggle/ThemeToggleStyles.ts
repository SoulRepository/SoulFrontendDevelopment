import {SystemStyleObject} from "@chakra-ui/react";

export const ThemeToggleStyles: SystemStyleObject = {
  border: '1px solid rgba(255, 255, 255, 0.2)',
  borderRadius: 'full',
  boxSize: '36px',
  alignItems: 'center',
  justifyContent: 'center',
}
